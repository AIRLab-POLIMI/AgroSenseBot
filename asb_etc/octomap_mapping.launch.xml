<launch>
	<node pkg="octomap_server" exec="octomap_server_node" name="octomap_server">
		<param name="resolution" value="0.05" />

		<!-- fixed map frame (set to 'map' if SLAM or localization running!) -->
		<param name="frame_id" value="map" />

		<!-- Minimum height of points to consider for insertion in the callback.
		Any point outside of this interval will be discarded before running any insertion or ground plane filtering.
		You can do a rough filtering based on height with this, but if you enable the ground_filter this interval needs
		to include the ground plane. -->
		<param name="point_cloud_min_z" value="0.2" />
		<param name="point_cloud_min_y" value="0.0" />

		<!-- maximum range to integrate (speedup!) -->
		<param name="sensor_model.max_range" value="1.0" />

		<!-- data source to integrate (PointCloud2) -->
		<remap from="cloud_in" to="/scan_front_right_multilayer/points" />
	</node>
</launch>
